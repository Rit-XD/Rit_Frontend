export const generatePasswordFromIcons = (base: string) => {
    // if (!process.env.PASSWORD_PREFIX) {
    //   throw new Error(
    //     'PASSWORD_PREFIX environment variable has to be set. Grab the last environment file from 1pw.'
    //   )
    // }
    return `${base}`
  }
  